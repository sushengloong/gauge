console.log('HELLO!');
<% if @goal.errors.any? %>

  var msg = '<div class="alert alert-error alert-block">'
    + '<button type="button" class="close" data-dismiss="alert">×</button>'
    + "<h4><%= pluralize(@goal.errors.count, "error") %> prohibited this goal from being saved:</h4>"
    + "<ul>"
    <% @goal.errors.full_messages.each do |msg| %>
      + "<li><%= msg %></li>"
    <% end %>
    + "</ul>"
    + "</div>";
  $('div#message-wrapper').html(msg);

<% else %>

  $('div#new-goal-ctn').modal('hide');

  var tbody = $('table#goals-table tbody');
  if (tbody.find('tr').not('.no-data').length == 0) {
    $('table#goals-table tbody').html("<%= escape_javascript render(@goal) %>");
  } else {
    $('table#goals-table tbody').append("<%= escape_javascript render(@goal) %>");
  }

<% end %>
